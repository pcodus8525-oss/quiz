# Quiz Play — PRD

> 작성일: 2026-09-15 · 버전: v1 (1차 릴리스 범위)

## 1. 제품 개요

- **제품명(가칭)**: Quiz Play
- **한 줄 정의**: 원하는 주제를 입력하면 AI가 즉석에서 퀴즈를 만들어주고, 점수를 랭킹으로 겨루는 싱글 플레이 웹 퀴즈 게임
- **핵심 가치**: 문제 은행을 사람이 채워 넣을 필요가 없다. 주제만 있으면 게임이 성립한다
- **타깃 사용자**: 짧은 시간에 가볍게 즐기려는 일반 사용자 (모바일 웹 우선)

### 결정 사항 요약

| 항목 | 결정 |
| --- | --- |
| 콘텐츠 | 주제 무관 범용 퀴즈 엔진 |
| 플레이 방식 | 싱글 플레이 + 랭킹 보드 |
| 문제 유형 | 4지선다 객관식, OX |
| 문제 소스 | OpenAI API 실시간 생성 + DB 캐시 재사용 |
| 주제 선택 | 추천 카테고리 카드 + 자유 입력 |
| 게임 설정 | 시작 전 문항 수·난이도를 유저가 선택 |
| 랭킹 | 로그인 없이 닉네임만 입력 |
| DB | Neon Postgres (Vercel 연동) |
| UI 톤 | 캔디한(캐주얼·컬러풀) 게임 느낌 |
| 1차 포함 | 오답 리뷰, 난이도 선택, 문제 신고 |
| 1차 제외 | 결과 공유, 이미지 문제, 주관식, 멀티플레이 |

## 2. 사용자 플로우

1. **홈** — 추천 카테고리 카드 선택 또는 주제 자유 입력 → 난이도(쉬움/보통/어려움) → 문항 수(5/10/20) → `시작`
2. **로딩** — 캐시된 문제를 조회하고 부족분만 OpenAI로 생성 (진행 애니메이션 노출)
3. **게임** — 문제 1개씩 표시, 보기 선택 → 정답/오답 즉시 피드백 → 다음 문제
4. **결과** — 총점·정답 수·소요 시간 표시 → 오답 리뷰(문제 + 정답 + 해설, 문제별 신고 버튼) → 닉네임 입력 후 랭킹 등록
5. **랭킹** — 난이도·문항 수 조건별 상위 기록 조회

## 3. 기능 요구사항

### 3.1 퀴즈 생성 (P0)

- **입력**: 주제(카테고리 또는 자유 텍스트), 난이도, 문항 수
- **문제 유형**: 4지선다 객관식과 OX를 섞어서 출제
- 각 문제는 `질문 / 보기 / 정답 / 해설`을 포함한다 (해설은 오답 리뷰에 사용)
- OpenAI API로 생성하며, 응답은 구조화된 스키마로 강제해 파싱 실패를 방지한다
- **캐시 전략**: 주제를 정규화한 슬러그 + 난이도를 키로 DB에 저장한다. 요청 시 캐시에서 먼저 채우고 **모자란 개수만** 새로 생성한다. 같은 주제를 반복 플레이해도 문제가 겹치지 않도록 무작위 추출한다
- 생성 실패 시 재시도 가능한 오류 화면을 보여준다

### 3.2 게임 플레이 (P0)

- 문항당 제한 시간 **20초**. 시간 초과 시 오답 처리 후 다음 문제로 진행
- **점수 공식**: `정답당 100점 × 난이도 배수(쉬움 1.0 / 보통 1.3 / 어려움 1.6)` + `남은 시간(초) × 5`
- 정답/오답 시 색상·모션 피드백, 진행도 바(예: 3/10) 표시
- **채점은 서버에서 수행한다.** 클라이언트에 정답을 내려주지 않아 랭킹 조작을 막는다

### 3.3 오답 리뷰 (P0)

- 결과 화면에서 틀린 문제 목록, 내가 고른 답, 정답, 해설을 함께 표시
- 문제별 **신고 버튼** 제공 → 누적 신고가 임계치를 넘으면 자동으로 캐시에서 숨김 처리

### 3.4 랭킹 보드 (P0)

- 로그인 없음. 결과 화면에서 닉네임(2~12자)만 입력해 등록
- 난이도 × 문항 수 조합별로 분리해 조회 (조건이 다른 기록끼리 섞이지 않게)
- 동점 시 총 소요 시간이 짧은 쪽이 상위
- 표시 항목: 순위, 닉네임, 주제, 점수, 정답 수, 소요 시간, 일시

### 3.5 비기능 요구사항

- 모바일 우선 반응형 (최소 360px 폭 지원)
- 문제 생성 대기 시간 목표: 캐시 히트 시 1초 이내, 신규 생성 시 15초 이내
- OpenAI 호출은 서버 라우트에서만 수행 (API 키 클라이언트 노출 금지)
- 동일 IP의 과도한 생성 요청에 대한 간단한 rate limit

### 3.6 1차 범위 제외

- 결과 공유(SNS·이미지 카드)
- 이미지 문제, 주관식(단답형) 문제
- 실시간 멀티플레이, 1:1 대결
- 소셜 로그인, 관리자 페이지

## 4. 기술 스택

| 영역 | 선택 |
| --- | --- |
| 프레임워크 | Next.js (App Router) + TypeScript |
| 스타일 | Tailwind CSS |
| DB | Neon Postgres (Vercel 연동, `DATABASE_URL`) |
| DB 접근 | `@neondatabase/serverless` |
| AI | AI SDK v6 (`ai` + `@ai-sdk/openai`)의 `generateObject` + Zod 스키마 |
| 배포 | Vercel |

**환경 변수**: `DATABASE_URL`, `OPENAI_API_KEY`, `OPENAI_MODEL`(기본값 지정, 교체 가능)

## 5. 데이터 모델 (Neon Postgres)

**questions**
`id`, `topic_slug`, `topic_label`, `difficulty`, `type`(`mc4` | `ox`), `prompt`, `choices`(jsonb), `answer_index`, `explanation`, `report_count`, `is_hidden`, `created_at`

**games**
`id`, `topic_slug`, `topic_label`, `difficulty`, `question_count`, `question_ids`(jsonb), `status`, `created_at`

**scores**
`id`, `game_id`, `nickname`, `topic_label`, `difficulty`, `question_count`, `score`, `correct_count`, `duration_ms`, `created_at`

**reports**
`id`, `question_id`, `reason`, `created_at`

**인덱스**
- `questions(topic_slug, difficulty, is_hidden)`
- `scores(difficulty, question_count, score DESC, duration_ms ASC)`

## 6. 화면 / API 구성

### 페이지

| 경로 | 설명 |
| --- | --- |
| `/` | 홈 — 주제·난이도·문항 수 선택 |
| `/play/[gameId]` | 게임 진행 |
| `/result/[gameId]` | 결과 + 오답 리뷰 + 닉네임 등록 |
| `/leaderboard` | 랭킹 보드 |

### API 라우트

| 메서드 · 경로 | 설명 |
| --- | --- |
| `POST /api/games` | 주제/난이도/문항 수로 문제 확보(캐시 + 생성), 게임 세션 생성, **정답을 뺀** 문제 목록 반환 |
| `POST /api/games/[id]/submit` | 답안 배열 채점, 점수·오답 리뷰 데이터 반환 |
| `POST /api/games/[id]/score` | 닉네임 등록 |
| `GET /api/leaderboard` | 난이도·문항 수 필터 조회 |
| `POST /api/reports` | 문제 신고 |

## 7. 디자인 방향

캔디한 캐주얼 게임 톤. 밝은 그라데이션 배경, 크고 둥근 보기 버튼, 정답 시 팝 애니메이션 / 오답 시 셰이크 애니메이션, 굵은 산세리프 타이포, 카운트다운 링. 색상은 정답 = 민트·그린, 오답 = 코럴·레드로 통일한다.

## 8. 확정된 세부 값

- **문항당 제한 시간 20초.** 시간 초과는 오답 처리하며, 남은 시간은 점수 보너스가 된다
- **추천 카테고리**: 역사, 과학, 스포츠, 영화·드라마, K-POP, 일반 상식 (자유 입력도 함께 지원)
- **문제 신고 임계치 3회.** 누적 3회 신고된 문제는 캐시에서 자동으로 숨겨져 다시 출제되지 않는다
- **저장소 이중화**: `DATABASE_URL`이 없으면 프로세스 메모리에, 있으면 Neon에 저장한다. 데모를 키 하나 없이 돌릴 수 있게 하기 위한 장치이며, 코드는 `src/lib/store/`의 동일한 인터페이스를 공유한다

## 9. 구현 순서

1. Next.js 스캐폴딩 (App Router + TypeScript + Tailwind)
2. Neon 연결 및 스키마 마이그레이션
3. 문제 생성 API (OpenAI + 캐시 로직)
4. 홈 / 게임 / 결과 화면
5. 랭킹 보드 및 문제 신고
6. Vercel 배포
